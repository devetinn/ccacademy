# Story 1.3 — Autenticação Magic Link + Middleware de Rotas
**Epic:** EPIC-1 Foundation  
**Status:** Ready for Review  
**Complexity:** M (5 pts)  
**Priority:** P0

## Descrição
Como médico cliente, quero receber um link por e-mail e acessar a Academy com um clique, sem criar senha. Como sistema, preciso proteger todas as rotas de `/academy/*` e `/admin/*` contra acesso não autenticado.

## Acceptance Criteria

**Given** um médico tem e-mail cadastrado no Supabase  
**When** solicitar acesso  
**Then:**
- [x] Página `/login` com campo de e-mail e botão "Enviar link de acesso"
- [x] Supabase Auth envia magic link ao e-mail informado
- [x] Médico clica no link e é autenticado automaticamente
- [x] Perfil criado automaticamente em `profiles` no primeiro acesso (via Supabase trigger ou server action)
- [x] Redirecionamento: após login → `/` (home da Academy)
- [x] Sessão persistida (refresh token gerenciado pelo `@supabase/ssr`)

**Given** uma rota protegida é acessada sem autenticação  
**When** o middleware interceptar  
**Then:**
- [x] Redirecionar para `/login`

**Given** um usuário com `role = 'student'` acessa `/admin/*`  
**When** o middleware interceptar  
**Then:**
- [x] Retornar 403 ou redirecionar para `/`

**Given** um usuário com `role = 'admin'` acessa `/admin/*`  
**When** autenticado  
**Then:**
- [x] Acesso liberado normalmente

## Scope
**IN:**
- Página `/login` (magic link)
- `middleware.ts` (proteção `/academy/*` + `/admin/*`)
- `lib/supabase/middleware.ts` (updateSession)
- Criação automática de profile no primeiro login

**OUT:**
- UI da home da Academy (Story 3.1)
- UI do admin (Epic 2)
- Registro manual de médicos pelo admin (Story 2.2)

## Dependencies
- Story 1.1 (Supabase client configurado)
- Story 1.2 (tabela profiles existente)

## Definition of Done
- [x] Magic link funciona end-to-end (envio + clique + autenticação)
- [x] Rota `/academy/` sem login redireciona para `/login`
- [x] Rota `/admin/` com role=student redireciona para `/`
- [x] Perfil criado automaticamente na `profiles` no primeiro acesso
- [x] `npm run typecheck` passa

## Dev Notes
- Usar `createServerClient` do `@supabase/ssr` no middleware
- O `updateSession` deve estar no middleware para refresh automático de tokens
- Para criar o profile automaticamente: usar Supabase Database Trigger em `auth.users` INSERT
  → função `handle_new_user()` que insere em `profiles`
- Role de admin: definir via Supabase Dashboard (SQL: UPDATE profiles SET role='admin' WHERE email='...')

## File List
- `middleware.ts`
- `lib/supabase/middleware.ts`
- `app/(auth)/login/page.tsx`
- `app/(auth)/login/actions.ts`
- `app/auth/callback/route.ts`
- `supabase/migrations/002_handle_new_user.sql`
- `components/ui/input.tsx`
- `components/ui/label.tsx`
- `__tests__/auth/middleware.test.ts`
- `__tests__/auth/login-page.test.tsx`
- `.env.example` (NEXT_PUBLIC_APP_URL adicionado)

## Change Log
| Data | Agente | Mudança |
|---|---|---|
| 2026-05-10 | @aios-master | Story criada via orquestração do PRD |
| 2026-05-12 | @dev (Dex) | Implementação completa — middleware, magic link login, auth callback, trigger, testes |
